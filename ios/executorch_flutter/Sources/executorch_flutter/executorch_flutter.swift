// ExecuTorch Flutter is a dart:ffi plugin: its native library is built and
// bundled through Flutter's native assets system (see hook/build.dart), not
// compiled by this Swift package or the CocoaPods podspec.
//
// This file exists only so the Swift Package Manager target is non-empty; the
// package's sole purpose is to advertise SPM support to the Flutter tool.
import Foundation

enum ExecutorchFlutter {}
