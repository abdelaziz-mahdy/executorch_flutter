// AUTO GENERATED FILE, DO NOT EDIT.
// Generated by ffigen.
//
// FFI bindings for the ExecuTorch LLM runner
// See: native/src/executorch_llm_ffi.h

// AUTO GENERATED FILE, DO NOT EDIT.
//
// Generated by `package:ffigen`.
// ignore_for_file: type=lint, unused_import
@ffi.DefaultAsset('package:executorch_dart/executorch_dart.dart')
library;

import 'dart:ffi' as ffi;
import 'package:executorch_dart/src/generated/executorch_ffi.g.dart' as imp$1;
import '' as self;

/// Free a token string handed to an ETTokenCallback. Safe to call with NULL.
@ffi.Native<ffi.Void Function(ffi.Pointer<ffi.Char>)>()
external void et_llm_string_free(ffi.Pointer<ffi.Char> token);

/// Create a runner from a model file and a tokenizer file.
///
/// The tokenizer format is auto-detected (HF tokenizer.json, TikToken,
/// SentencePiece, or BPE). The model is mmap'd (with mlock-if-available) so large
/// weights are not copied into RAM.
///
/// @param model_path     Path to the .pte model file (required).
/// @param tokenizer_path Path to the tokenizer file (required).
/// @param data_path      Optional path to a .ptd weight blob produced by some exports;
/// pass NULL when weights are embedded in the .pte.
/// @param out            Out-param; on success receives a non-NULL ETLLMRunner*.
/// @return ETStatus*     code==ET_OK on success; otherwise an error. Caller frees.
@ffi.Native<
  ffi.Pointer<imp$1.ETStatus> Function(
    ffi.Pointer<ffi.Char>,
    ffi.Pointer<ffi.Char>,
    ffi.Pointer<ffi.Char>,
    ffi.Pointer<ffi.Pointer<ETLLMRunner>>,
  )
>()
external ffi.Pointer<imp$1.ETStatus> et_llm_runner_create(
  ffi.Pointer<ffi.Char> model_path,
  ffi.Pointer<ffi.Char> tokenizer_path,
  ffi.Pointer<ffi.Char> data_path,
  ffi.Pointer<ffi.Pointer<ETLLMRunner>> out,
);

/// Eagerly load the model and prepare for inference. Optional — generate() will load
/// lazily on first call. Useful to surface load errors / warm up ahead of time.
@ffi.Native<ffi.Pointer<imp$1.ETStatus> Function(ffi.Pointer<ETLLMRunner>)>()
external ffi.Pointer<imp$1.ETStatus> et_llm_runner_load(
  ffi.Pointer<ETLLMRunner> runner,
);

/// @return 1 if the runner has loaded its model, 0 otherwise (0 if runner is NULL).
@ffi.Native<ffi.Int32 Function(ffi.Pointer<ETLLMRunner>)>(isLeaf: true)
external int et_llm_is_loaded(ffi.Pointer<ETLLMRunner> runner);

/// Free the runner and all owned resources. Safe to call with NULL.
/// Do not call concurrently with an in-flight generate on the same runner.
@ffi.Native<ffi.Void Function(ffi.Pointer<ETLLMRunner>)>()
external void et_llm_runner_free(ffi.Pointer<ETLLMRunner> runner);

/// Synchronous streaming generation. Invokes `token_cb` for each generated UTF-8
/// piece on the CALLING thread, returning when generation finishes (EOS / token
/// limit) or et_llm_stop() is called. Intended for the native C test harness.
///
/// @return ETStatus* code==ET_OK on success; otherwise an error. Caller frees.
@ffi.Native<
  ffi.Pointer<imp$1.ETStatus> Function(
    ffi.Pointer<ETLLMRunner>,
    ffi.Pointer<ffi.Char>,
    ffi.Pointer<ETGenConfig>,
    ETTokenCallback,
    ffi.Pointer<ffi.Void>,
  )
>()
external ffi.Pointer<imp$1.ETStatus> et_llm_generate(
  ffi.Pointer<ETLLMRunner> runner,
  ffi.Pointer<ffi.Char> prompt,
  ffi.Pointer<ETGenConfig> cfg,
  ETTokenCallback token_cb,
  ffi.Pointer<ffi.Void> user_data,
);

/// Asynchronous streaming generation. Runs the decode loop on an internal C++ thread
/// and invokes `token_cb` per piece (use Dart `NativeCallable.listener` so tokens are
/// marshalled to the Dart isolate). When generation ends, `done_cb` is invoked exactly
/// once with the final `ETStatus*` (cast the void* arg to ETStatus*); the receiver owns
/// it and must call et_status_free(). Mirrors et_module_forward_async in executorch_ffi.h.
///
/// @param token_user_data Passed through to every token_cb invocation.
/// @param done_cb         Completion callback; receives ETStatus* as its void* argument.
@ffi.Native<
  ffi.Void Function(
    ffi.Pointer<ETLLMRunner>,
    ffi.Pointer<ffi.Char>,
    ffi.Pointer<ETGenConfig>,
    ETTokenCallback,
    ffi.Pointer<ffi.Void>,
    imp$1.ETCallback_1,
  )
>()
external void et_llm_generate_async(
  ffi.Pointer<ETLLMRunner> runner,
  ffi.Pointer<ffi.Char> prompt,
  ffi.Pointer<ETGenConfig> cfg,
  ETTokenCallback token_cb,
  ffi.Pointer<ffi.Void> token_user_data,
  imp$1.ETCallback_1 done_cb,
);

/// Cooperatively stop an in-flight generation. Safe to call from a different thread
/// than the one running generate(). No-op if nothing is generating.
@ffi.Native<ffi.Void Function(ffi.Pointer<ETLLMRunner>)>()
external void et_llm_stop(ffi.Pointer<ETLLMRunner> runner);

/// Clear the KV cache and reset the conversation start position (begin a fresh
/// conversation). Do not call during an in-flight generation.
@ffi.Native<ffi.Void Function(ffi.Pointer<ETLLMRunner>)>()
external void et_llm_reset(ffi.Pointer<ETLLMRunner> runner);

/// Set the absolute path to the MLX Metal kernel library (mlx.metallib).
///
/// The MLX backend loads its precompiled Metal kernels (`mlx.metallib`) at the
/// first GPU op by searching next to the loaded binary, a couple of Resources/
/// subdirs, and finally a compile-time absolute path. None of those resolve when
/// the library is bundled inside a sandboxed Flutter app (the .metallib is shipped
/// as a Flutter data asset, not next to the dylib). Call this BEFORE creating a
/// runner for an MLX model so the (patched) MLX loader can find the metallib via
/// the ET_MLX_METALLIB_PATH environment variable. No-op / harmless for non-MLX
/// models. Passing NULL or "" clears it.
///
/// Thread-safety: sets a process environment variable; call once at startup or
/// before et_llm_runner_create, not concurrently with generation.
@ffi.Native<ffi.Void Function(ffi.Pointer<ffi.Char>)>()
external void et_llm_set_metallib_path(ffi.Pointer<ffi.Char> path);

const addresses = _SymbolAddresses();

class _SymbolAddresses {
  const _SymbolAddresses();
  ffi.Pointer<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ETLLMRunner>)>>
  get et_llm_runner_free => ffi.Native.addressOf(self.et_llm_runner_free);
}

final class ETLLMRunner extends ffi.Opaque {}

/// Generation parameters. Field semantics mirror
/// executorch::extension::llm::GenerationConfig EXACTLY.
///
/// Defaults (when constructing on the Dart side): max_new_tokens=-1, seq_len=-1,
/// temperature=0.8, echo=0, ignore_eos=0, num_bos=0, num_eos=0.
final class ETGenConfig extends ffi.Struct {
  /// < Max new tokens; -1 => derive from model max_context_len.
  @ffi.Int32()
  external int max_new_tokens;

  /// < Max total tokens; -1 => use model metadata.
  @ffi.Int32()
  external int seq_len;

  /// < Sampling temperature. <= 0 behaves greedily (argmax).
  @ffi.Float()
  external double temperature;

  /// < Bool (0/1): echo the prompt in the output stream.
  @ffi.Int32()
  external int echo;

  /// < Bool (0/1): keep generating past EOS up to the limit.
  @ffi.Int32()
  external int ignore_eos;

  /// < Number of BOS tokens to prepend to the prompt.
  @ffi.Int32()
  external int num_bos;

  /// < Number of EOS tokens to append to the prompt.
  @ffi.Int32()
  external int num_eos;
}

/// Per-token streaming callback.
///
/// @param token     Heap-allocated, NUL-terminated UTF-8 token piece. OWNERSHIP
/// TRANSFERS to the callback: the receiver MUST free it with
/// et_llm_string_free(). (Heap ownership — rather than a transient
/// pointer — is required so the string survives the asynchronous hop
/// to the Dart isolate via NativeCallable.listener.)
/// @param user_data Opaque pointer passed through from the generate call.
typedef ETTokenCallback =
    ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<ffi.Char> token,
          ffi.Pointer<ffi.Void> user_data,
        )
      >
    >;
