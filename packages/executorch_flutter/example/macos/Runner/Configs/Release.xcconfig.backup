#include "../../Flutter/Flutter-Release.xcconfig"
#include "Warnings.xcconfig"

// Exclude x86_64 architecture for dependencies that only support arm64 (Apple Silicon)
// This allows building on Apple Silicon Macs when dependencies like ExecuTorch don't support Intel
EXCLUDED_ARCHS = x86_64
