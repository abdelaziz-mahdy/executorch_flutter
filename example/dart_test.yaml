# Test configuration for integration tests
# The default load_timeout (12 minutes) is too short for CI runners
# where flutter test -d <device> rebuilds native assets during the loading phase.
load_timeout: 1800  # 30 minutes in seconds
